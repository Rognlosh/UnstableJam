class_name LevitationQuirk
extends ItemQuirk

## Левитация: вещь ведёт себя как поплавок — её тянет вверх сила, которая
## то чуть пересиливает вес, то отпускает, и вещь медленно всходит и оседает.
##
## Сделано силой, а не масштабом гравитации, именно ради веса: gravity_scale
## облегчает вещь целиком, и придавить её сверху нечем — легчает и она,
## и всё, что на ней лежит. Подъёмная же сила конечна: ларец, поставленный
## на кубок, добавляет к контакту свои килограммы, а подъём остаётся прежним,
## и кубок не трогается с места. Это и делает укладку выбором.
##
## Сила считается в долях собственного веса тела, поэтому одно и то же
## свойство одинаково себя ведёт и на кубке, и на большой амфоре.

## Средняя подъёмная сила в долях веса. Меньше единицы намеренно: при 1.0
## и выше вещь ушла бы со стеллажа в потолок ещё до погрузки. При 0.95 она
## в среднем лежит, а всплывает лишь на части цикла.
@export_range(0.0, 2.0, 0.05) var lift_base: float = 0.95
## Размах колебания. 0.35 при базе 0.95 даёт разброс от 0.6 (оседает)
## до 1.3 (всходит).
@export_range(0.0, 1.0, 0.05) var lift_amplitude: float = 0.35
## Период дыхания подъёмной силы, с.
@export var lift_period: float = 3.6

## Сопротивление среды по вертикали, 1/с. Оно и делает движение медленным:
## при избытке подъёма в 0.3g установившаяся скорость выходит около 50 px/с
## вместо разгона в свободном полёте.
@export var rise_drag: float = 6.0
## Потолок скорости, до которого сопротивление вообще работает. Без него
## левитация превратилась бы в подушку безопасности: любое падение гасилось
## бы досуха, и разбить вещь стало бы невозможно.
@export var drag_cap: float = 140.0

## Боковой снос, px/с². Среднее за период равно нулю: вещь водит вокруг
## своего места, а не сдувает в одну сторону. По умолчанию выключен —
## на узкой подставке он читается как качание, а не как парение.
@export var drift_accel: float = 0.0
@export var drift_period: float = 2.4

## Гашение медленного бокового хода, 1/с. Почти невесомая вещь давит
## на опору пятью процентами своего веса, поэтому трения у неё практически
## нет: она ползёт по полке от любого шума контакта, тем более что спать
## мы ей запретили. Гасим этот ход сами.
@export var creep_damp: float = 8.0
## До какой скорости гашение работает. Порог низкий намеренно: в кузове
## вещь едет вместе с машиной на сотнях px/с, и тормозить это движение
## нельзя — иначе груз просто уползёт к заднему борту.
@export var creep_speed: float = 30.0
## Вращение гасим: парящая вещь, крутящаяся волчком, читается как ошибка.
@export var angular_damp: float = 1.5

## Временный вывод в консоль раз в секунду: подъём, ускорение, сон.
@export var debug: bool = false


func create_runtime() -> RefCounted:
	return Runtime.new()


## Вложенный класс — как nested class в C#. Держим поведение рядом с его
## настройками: искать их по двум файлам смысла нет.
class Runtime extends QuirkRuntime:
	var _time: float = 0.0
	## Сдвиг фазы: без него все парящие вещи в кузове дышали бы
	## синхронно, как одно тело.
	var _phase: float = 0.0
	var _debug_time: float = 0.0

	func _setup() -> void:
		var cfg := quirk as LevitationQuirk
		if cfg == null:
			return
		# gravity_scale не трогаем вовсе: вес должен остаться настоящим,
		# иначе давить сверху будет нечем.
		item.angular_damp = lerpf(0.0, cfg.angular_damp, strength)
		_phase = randf() * TAU

	func physics_step(state: PhysicsDirectBodyState2D) -> void:
		var cfg := quirk as LevitationQuirk
		if cfg == null:
			return
		var gravity := state.total_gravity
		if gravity.is_zero_approx():
			return
		_time += state.step

		var up := -gravity.normalized()
		# Ускорение, а не сила: умножение на массу — в самом конце, поэтому
		# цифры в ресурсе не зависят от того, на какой предмет их надели.
		var accel := Vector2.ZERO

		# Подъём. Прикладываем ВСЮ подъёмную силу, а не её избыток над весом:
		# вес движок вычел из скорости сам, ещё до этого вызова. Прибавь мы
		# только избыток — тело получило бы вычет веса дважды и не взлетело
		# бы ни при какой амплитуде.
		var lift: float = cfg.lift_base
		if cfg.lift_period > 0.0:
			lift += cfg.lift_amplitude * sin(_time * TAU / cfg.lift_period + _phase)
		accel += -gravity * lift * strength

		# Сопротивление по вертикали — только в пределах drag_cap.
		var vertical := state.linear_velocity.dot(up)
		accel += up * (-cfg.rise_drag * strength \
			* clampf(vertical, -cfg.drag_cap, cfg.drag_cap))

		var side := Vector2(up.y, -up.x)

		# Боковой снос. Частота своя, несоизмеримая с подъёмом: иначе
		# движение замкнулось бы в аккуратный овал и читалось бы заводным.
		if cfg.drift_period > 0.0 and cfg.drift_accel > 0.0:
			accel += side * cfg.drift_accel * strength \
				* sin(_time * TAU / cfg.drift_period + _phase * 1.7)

		# Гашение ползания. Только для медленного хода: быстрый — это либо
		# езда вместе с кузовом, либо удар, и трогать его нельзя.
		var lateral := state.linear_velocity.dot(side)
		if absf(lateral) < cfg.creep_speed:
			accel += side * (-cfg.creep_damp * strength * lateral)

		# Интегрируем сами, через скорость, а не через apply_central_force:
		# сила, поданная из _integrate_forces, применяется относительно шага
		# солвера со сдвигом, и эффект легко потерять. Прибавка к скорости
		# идёт до решения контактов, поэтому вес по-прежнему учитывается:
		# поставленный сверху ларец эту прибавку гасит.
		state.linear_velocity += accel * state.step

		if cfg.debug:
			_debug_time += state.step
			if _debug_time >= 1.0:
				_debug_time = 0.0
				print("[левитация] подъём %.2f · итог %.0f · скорость %.0f · спит %s"
					% [lift, accel.y + gravity.y, state.linear_velocity.y,
					item.sleeping])
